{
  "name": "parental-benefit-calculator-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "serve": "http-server . -p 5500 -c-1",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.9.2"
  }
}
